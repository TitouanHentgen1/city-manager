# AI Usage — All Phases

**AI Tool used:** Claude (claude.ai)

---

## Phase 1

### Function: `parse_condition`

**Prompt given:**
> "Generate a C function `int parse_condition(const char *input, char *field, char *op, char *value)` that splits a `field:op:value` string into three parts."

**Code generated by AI:**
```c
int parse_condition(const char *input, char *field, char *op, char *value) {
    const char *colon1 = strchr(input, ':');
    if (!colon1) return 0;
    size_t flen = colon1 - input;
    strcpy(field, input, flen);
    field[flen] = '\0';
    const char *colon2 = strchr(colon1 + 1, ':');
    if (!colon2) return 0;
    size_t olen = colon2 - colon1 - 1;
    strcpy(op, colon1 + 1, olen);
    op[olen] = '\0';
    strcpy(value, colon2 + 1, 63);
    value[63] = '\0';
    return 1;
}
```

**Changes made:**
- Replaced `strcpy` with `strncpy` to prevent buffer overflows, since `strcpy` does not take a size limit
- Added operator validation: the AI did not check whether the operator was one of the six supported values (`==`, `!=`, `<`, `<=`, `>`, `>=`); an explicit check with an error message was added

---

### Function: `match_condition`

**Prompt given:**
> "Generate a C function `int match_condition(Report *r, const char *field, const char *op, const char *value)` that returns 1 if the report matches the condition."

**Code generated by AI:**
```c
int match_condition(Report *r, const char *field, const char *op, const char *value) {
    if (strcmp(field, "severity") == 0) {
        int rval = r->severity;
        int cval = atoi(value);
        if (strcmp(op, "==") == 0) return rval == cval;
        if (strcmp(op, "!=") == 0) return rval != cval;
        if (strcmp(op, "<")  == 0) return rval <  cval;
        if (strcmp(op, "<=") == 0) return rval <= cval;
        if (strcmp(op, ">")  == 0) return rval >  cval;
        if (strcmp(op, ">=") == 0) return rval >= cval;
    }
    if (strcmp(field, "timestamp") == 0) {
        time_t rval = r->timestamp;
        time_t cval = (time_t)atol(value);
        /* comparison logic missing */
    }
    if (strcmp(field, "category") == 0) {
        int cmp = strcmp(r->category, value);
        if (strcmp(op, "==") == 0) return cmp == 0;
        if (strcmp(op, "!=") == 0) return cmp != 0;
        /* remaining operators missing */
    }
    /* inspector field missing entirely */
}
```

**Changes made:**
- `timestamp` field was incomplete: the AI declared the variables but left the comparison body empty — all six operators were added manually
- `category` and `inspector` only handled `==` and `!=` — the four remaining comparison operators were added
- The `inspector` field was missing entirely from the AI output — the full branch was written manually
- The repeated comparison logic was refactored into two static helper functions `cmp_int` and `cmp_str` to avoid duplication across fields

---

### What I learned

The AI gave a usable skeleton but missed several cases. Reviewing line by line before integrating is necessary.

---

## Phase 2

### `monitor_reports` — signal handlers

**Question asked:**
> "In a C signal handler, can I use `printf`? What should I use instead, and why?"

**Code suggested by AI:**
```c
static void handler_sigusr1(int sig) {
    (void)sig;
    const char *msg = "[monitor] Report added\n";
    write(STDOUT_FILENO, msg, strlen(msg));
}

static void handler_sigint(int sig) {
    (void)sig;
    const char *msg = "[monitor] SIGINT received — shutting down.\n";
    write(STDOUT_FILENO, msg, strlen(msg));
    unlink(PID_FILE);
    exit(0);
}
```

**Changes made:**
- `exit(0)` was replaced with `_exit(0)` in `handler_sigint`: `exit()` is not async-signal-safe because it flushes stdio buffers and runs `atexit` handlers that may conflict with interrupted code. `_exit()` terminates immediately and is safe to call from a signal handler
- Message text was updated to match the final prefixed format (`END:`, `MSG:`) used in Phase 3

---

### `monitor_reports` — main function

**Question asked:**
> "Show me how to use `sigaction` to install a signal handler for SIGUSR1 that restarts interrupted system calls, and a handler for SIGINT that does not."

**Code suggested by AI:**
```c
struct sigaction sa;
sa.sa_handler = handler_sigusr1;
sigemptyset(&sa.sa_mask);
sa.sa_flags = SA_RESTART;
sigaction(SIGUSR1, &sa, NULL);

sa.sa_handler = handler_sigint;
sigemptyset(&sa.sa_mask);
sa.sa_flags = 0;
sigaction(SIGINT, &sa, NULL);

while (1) pause();
```

**Changes made:**
- The overall structure was correct and kept as-is
- Verified that `SA_RESTART` is set for `SIGUSR1` (so `pause()` restarts after the handler returns) and not for `SIGINT` (since the handler calls `_exit()` and never returns)
- Error handling added on each `sigaction` call — the AI version had none
- The `signal()` call was never used, in compliance with the project requirement to use only `sigaction()`

---

### What I learned

Useful for understanding async-signal-safe rules, but the `exit` vs `_exit` distinction needed extra checking on my side.

---

## Phase 3

### `city_hub` — dup2 and exec interaction

**Question asked:**
> "After `dup2(pipefd[1], STDOUT_FILENO)` and then `exec`, does the child's `printf` go into the pipe?"

The AI confirmed yes — file descriptors survive `exec`, so the child's stdout is the pipe. It also pointed out that the original `pipefd[1]` must be closed after `dup2`, otherwise the pipe never receives EOF and the parent's `read` blocks forever.

**Applied:** Both in `run_hub_mon` (for `monitor_reports`) and in `cmd_calculate_scores` (for the scorer), `dup2` is followed immediately by closing the original write-end fd before calling `exec` or `run_scorer`.

---

### `city_hub` — reading lines from a pipe

**Question asked:**
> "What is the safest way to read complete lines from a pipe when I don't know the line length?"

The AI suggested using `fdopen` + `fgets`. I tried it but noticed it introduced buffering delays — messages from the monitor were not showing up immediately in the terminal. I switched to reading one byte at a time with `read()` until hitting a `\n`, which gives immediate delivery. The AI's suggestion was a starting point but I ended up doing it differently based on actual observed behavior.

---

### What I learned

Helpful for confirming syscall behavior, but the overall architecture was designed and written entirely by me.
